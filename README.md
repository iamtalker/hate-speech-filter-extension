# 혐오표현 필터 (Hate Speech Filter)

지역·성별·인종 등에 대한 혐오·비하 표현이 포함된 글을 자동으로 감지해 블러 처리하는 크롬 확장프로그램입니다.

**Chrome 웹 스토어에서 설치**: https://chromewebstore.google.com/detail/%ED%98%90%EC%98%A4%ED%91%9C%ED%98%84-%ED%95%84%ED%84%B0/nifmepekakeclkfiolnnlgmfhfmigihk

## 왜 "차단"이 아니라 "필터"인가

혐오·차별 표현을 막으려는 시도는 구조적인 한계가 있습니다. 말할 능력과 의지가 있는 사람을 강제로 틀어막는 건 사실상 불가능에 가깝고, 정당하든 아니든 그 자체를 원천 봉쇄하는 건 비효율적입니다. 계정을 막으면 새 계정을 만들고, 특정 단어를 막으면 초성이나 은어로 우회하고, 한 플랫폼을 규제하면 다른 곳으로 옮겨갈 뿐입니다.

그래서 이 확장 프로그램은 표현 자체를 막으려 하지 않습니다. 대신 무엇이 바람직하지 않은 표현인지 정의하고, 그것을 보지 않을 수단을 보는 사람에게 쥐여주는 쪽을 택했습니다. 말하는 사람을 통제하는 게 아니라, 보는 사람에게 시야를 선택할 권리를 돌려주는 것입니다. (그래도 지역명·성별·국적 같은 중립적인 단어 자체는 절대 걸러내지 않는다는 원칙은 지킵니다 — 아래 설계 원칙 참고)

## 설계 원칙

### 왜 "전라도", "여성" 같은 단어 자체는 차단하지 않는가

가장 단순하게 만들려면 "전라도"라는 단어 자체를 금지어로 등록하면 됩니다. 하지만 그러면 "전라도 여행 다녀왔어요", "전라도 음식 맛집 추천" 같은 아무 문제 없는 문장까지 전부 가려지고 맙니다. "전라도"는 지역을 가리키는 정당한 이름일 뿐이고, 죄가 있는 건 그 단어가 아니라 그 단어를 이용해 비하하는 맥락입니다. 단어 자체를 막아버리면 혐오표현 필터링이 아니라 그 단어에 대한 검열이 되어버립니다.

그래서 "여성", "전라도", "중국인" 같은 **집단 식별어는 그 자체로는 절대 차단 대상에 넣지 않습니다.**

### 대신 두 가지 조건으로만 판단합니다

1. **명백한 멸칭/비하 합성어** — "전라디언", "한남충"처럼 그 단어 자체가 이미 비하 목적 외에는 쓰일 일이 거의 없는 단어는 등장 즉시 차단합니다.
2. **중립 단어 + 모호한 비하 표현의 조합** — "홍어"는 생선 이름이기도 하지만, 지역차별 맥락에서 멸칭으로도 쓰입니다. 이런 단어는 단독으로는 차단하지 않고, "전라도" 같은 중립 단어와 **같은 문단 안에 함께** 등장할 때만 차단합니다.

예를 들어 "전라도 홍어 맛집 추천"은 안 걸리지만(생선 얘기), "전라도는 다 홍어다"는 걸립니다(멸칭 조합). **똑같은 단어("홍어")라도 옆에 뭐가 같이 있는지에 따라 판단이 갈리는 것**이 이 필터의 핵심 로직입니다.

전부 로컬 정규식 매칭으로만 동작하며, 외부 API 호출이나 LLM 추론은 사용하지 않습니다.

## 설치 방법

가장 쉬운 방법은 위 Chrome 웹 스토어 링크에서 "Chrome에 추가"를 누르는 것입니다.

직접 빌드해서 최신 개발 버전을 쓰고 싶다면 개발자 모드로 설치할 수 있습니다.

1. `chrome://extensions` 접속
2. 우측 상단 "개발자 모드" 켜기
3. "압축해제된 확장 프로그램을 로드합니다" 클릭
4. 이 폴더(`hate-speech-filter-extension`) 선택

## 기능

- 표시 방식 선택 가능: **블러 처리**(흐리게, 클릭하면 보기) / **숨기기**(안 보임, 클릭하면 보기) / **완전 삭제**(배지도 없이 흔적 없이 사라짐, 복구 불가)
- 카테고리를 직접 생성/삭제/이름 변경 가능 (지역차별·성차별 같은 고정 카테고리가 아니라 전부 사용자 데이터)
- 카테고리별 on/off
- 카테고리별로 집단 식별어 / 명백한 멸칭 / 모호한 표현 단어를 각각 확인하고 직접 추가·삭제 가능
- "기본 카테고리로 초기화"로 최초 제공되던 지역/성별/인종·국적/장애 카테고리로 되돌리기 가능
- "전체 삭제"로 카테고리를 한 번에 모두 비우기 가능
- 카테고리(단어 설정)를 JSON 파일로 내보내기/가져오기 가능 (백업, 다른 기기/사용자와 공유)
- 제외 사이트 등록 가능 — 등록한 사이트(및 서브도메인)에서는 확장 프로그램이 아예 동작하지 않음, "현재 보고 있는 사이트 추가" 버튼으로 원클릭 등록
- 감지된 글은 삭제가 아니라 블러 처리 후 클릭하면 다시 볼 수 있음

## 폴더 구조

```
manifest.json      확장프로그램 설정 (Manifest V3)
wordlists.js        최초 설치 시 시드로 쓰이는 기본 카테고리/단어 목록
settings-utils.js   설정 정규화, 구버전 데이터 마이그레이션 (공통 유틸)
content.js          페이지 스캔 및 블러 처리 로직
background.js       설치 시 기본 설정 초기화
popup.html/js/css   팝업 설정 UI (카테고리 생성/삭제/단어 편집)
styles.css          블러/배지 스타일
icons/              툴바/스토어용 아이콘 (16/48/128px, 확장프로그램 패키지에 포함됨)
store-assets/        스토어 등록 전용 이미지 (프로모션 타일 등, 패키지에는 미포함)
package.ps1          Chrome 웹 스토어 업로드용 zip 생성 스크립트 (dist/에 생성)
PRIVACY.md           개인정보처리방침 (스토어 등록용)
STORE_LISTING.md      스토어 등록용 설명문/체크리스트 초안
```

## Chrome 웹 스토어 등록

1. `powershell -File package.ps1` 실행 → `dist/`에 업로드용 zip 생성
2. [Chrome 웹 스토어 개발자 콘솔](https://chrome.google.com/webstore/devconsole)에서 계정 등록 (최초 1회 5달러)
3. zip 업로드, `STORE_LISTING.md`의 설명문/개인정보처리방침 URL 입력, 스크린샷 첨부
4. 심사 제출

자세한 체크리스트는 [STORE_LISTING.md](STORE_LISTING.md) 참고.

## 알려진 한계

- 규칙 기반이라 신조어·변형 표현은 목록에 없으면 걸러지지 않습니다. 팝업에서 사용자 지정 단어를 추가해 보완할 수 있습니다.
- 설정을 바꾸면 새로고침 후 적용됩니다.

## 변경 이력

버전별 상세 내용은 [Releases](https://github.com/iamtalker/hate-speech-filter-extension/releases)에서도 확인할 수 있습니다.

### v1.7.3 (버그 수정)
- **"리프 블록" 판정 로직 개선.** `<a class="title"><span>[짤방]</span>전라디언이 또 사고쳤노</a>`처럼 작은 분류 태그(span)가 앞에 붙어있고 실제 제목 텍스트는 별도 태그 없이 바로 붙어있는 구조에서, 내부에 span이 있다는 이유만으로 통째로 검사 대상에서 제외되던 문제 수정. 이제 자기 자신에게 직접 딸린 텍스트가 있으면 내부에 다른 태그가 있어도 검사 대상에 포함됨 (일베 검색결과 제목 등에서 확인)

### v1.7.2 (버그 수정)
- **`<a>`(링크) 태그가 스캔 대상에서 빠져있던 문제 수정.** 디시인사이드 검색결과 등 게시글 제목이 `<a class="...">제목</a>` 형태로만 되어있는 경우, 제목 자체는 검사 후보에도 안 들어가서 본문은 가려지는데 제목은 그대로 노출되는 문제가 있었음. `BLOCK_SELECTOR`에 `a` 추가
- 배지가 링크 안에 들어간 경우 배지 클릭 시 "보기/가리기" 대신 그 게시글로 이동해버리는 것을 방지 (`preventDefault` 추가)

### v1.7.1
- 팝업 표시 방식 라벨 "완전히 숨기기" → "숨기기"로 변경 ("완전 삭제"와 헷갈린다는 피드백 반영)

### v1.7.0
- "완전 삭제" 표시 방식 추가. 배지·클릭 복구 기능 없이 감지된 요소를 그냥 화면에서 사라지게 함(CSS로 숨김, DOM에서 실제로 제거하지는 않음). 블러/완전히 숨기기와 달리 되돌릴 수 없으므로 팝업에 경고 색상으로 표시

### v1.6.0
- 표시 방식 옵션 추가. 기존에는 블러 처리만 가능했는데, 이제 팝업 상단에서 "블러 처리" / "완전히 숨기기" 중 선택 가능. 완전히 숨기기를 선택하면 감지된 내용이 아예 렌더링되지 않고(배지는 그대로 남아 클릭하면 볼 수 있음)

### v1.5.0
- 제외 사이트 기능 추가. 팝업 하단에 사이트를 등록하면(서브도메인 포함) 그 사이트에서는 콘텐츠 스캔 자체를 하지 않음
- "현재 보고 있는 사이트 추가" 버튼으로 지금 열려있는 탭의 도메인을 바로 등록 가능
- URL을 붙여넣거나 도메인만 입력해도 자동으로 정규화(프로토콜/www/경로 제거)

### v1.4.2
- 기본 내장 단어 목록 확장 (`wordlists.js`)
  - 지역차별: 경상도 계열 중립어 추가, "과메기"를 모호 표현으로 추가
  - 성차별: "페미/페미니스트" 중립어, "김여사"·"메갈년" 명백한 멸칭 추가
  - 인종·국적차별: 필리핀·무슬림·난민·북한이탈주민 관련 중립어, "왜놈"·"쪽발이"·"난민충"·"무슬림충" 멸칭 추가, "짜장"·"빨갱이"를 모호 표현으로 추가
  - 장애 비하: "애자" 명백한 멸칭 추가, "병신"을 모호 표현으로 추가(단독으로는 오탐이 심해 "장애인" 등과 함께 나올 때만 차단)
- **주의**: 이미 설치되어 카테고리를 커스터마이징한 사용자에게는 자동으로 반영되지 않습니다. 팝업의 "기본 카테고리로 초기화"를 눌러야 새 단어가 적용됩니다 (단, 직접 추가/삭제한 내용은 초기화됨)

### v1.4.1
- 단어 추가/삭제 시 팝업이 닫혀버리는 문제 수정. 단어를 하나 추가/삭제할 때마다 카테고리 목록 전체를 다시 그리면서 열려있던 상세 보기가 전부 닫히고 레이아웃이 크게 흔들려 팝업이 꺼졌던 것 — 이제 해당 태그만 DOM에서 직접 추가/삭제하도록 수정. "+" 클릭/Enter 입력 후에도 입력창에 포커스가 유지되어 연속으로 단어를 추가하기 편해짐

### v1.4.0
- 카테고리(단어 설정) 내보내기/가져오기 기능 추가. 팝업에서 "내보내기"로 JSON 파일 저장, "가져오기"로 다른 기기/다른 사람의 단어 설정을 불러올 수 있음 (가져오면 현재 카테고리를 전부 대체, 확인 절차 포함)

### v1.3.3 (중요 버그 수정)
- **무한 루프로 인한 메모리 폭증 문제 수정.** 혐오표현이 감지되어 블러 처리된 글이 있으면, 블러 처리 시 삽입한 래퍼(`.hsf-inner`)가 MutationObserver에 의해 다시 스캔 대상이 되어 재귀적으로 계속 블러 래퍼를 겹겹이 씌우는 버그가 있었음. 이미 블러 처리된 내용물은 재스캔에서 제외하도록 수정
- 이 버전 이전에 설치했다면 열려있는 탭을 새로고침하거나 크롬을 재시작해야 이미 진행 중인 문제가 해소됨

### v1.3.2
- 아이콘을 스토어 가이드라인에 맞게 재작업 (96×96 그림 + 16px 투명 여백)
- 스토어용 작은 프로모션 타일(440×280) 추가 (`store-assets/`)
- `STORE_LISTING.md`에 필요한 이미지 규격/체크리스트 보강

### v1.3.1
- 아이콘 추가(16/48/128px), 툴바 아이콘 및 스토어용 아이콘으로 연결
- Chrome 웹 스토어 등록 준비: 개인정보처리방침(`PRIVACY.md`), 등록용 설명문(`STORE_LISTING.md`), 업로드용 zip 패키징 스크립트(`package.ps1`) 추가

### v1.3.0
- 카테고리 섹션에 "전체 삭제" 버튼 추가 (모든 카테고리를 한 번에 비움, 확인 절차 포함)

### v1.2.1
- 팝업 카테고리 단어 목록 표시 순서 변경: "명백한 멸칭"을 맨 위로 이동

### v1.2.0
- 카테고리를 고정 목록에서 자유 생성/삭제/이름 변경 가능한 구조로 변경
- 팝업에서 "새 카테고리 추가", 카테고리별 삭제, 라벨 인라인 수정 지원
- "기본 카테고리로 초기화" 기능 추가 (전체 카테고리 대상)
- 기존 사용자 지정 차단 단어 목록은 별도 섹션 대신 하나의 카테고리로 통합
- 구버전(v1.1.0 이하) 설정 자동 마이그레이션

### v1.1.0
- 팝업에서 카테고리별 단어 목록(집단 식별어 / 명백한 멸칭 / 모호한 표현) 확인 및 개별 추가·삭제 기능 추가
- 카테고리별 "기본값으로 초기화" 기능 추가
- 블러 배지 텍스트 오타 수정 ("혜오표현" → "혐오표현")
- 설정 병합 로직을 `settings-utils.js`로 분리

### v1.0.0
- 최초 릴리즈: 규칙 기반 혐오표현 감지 및 블러 처리
- 카테고리별 on/off, 사용자 지정 차단 단어 기능
- 팝업에서 카테고리별 기본 단어 목록 확인 기능
